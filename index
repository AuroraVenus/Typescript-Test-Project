const demoFunc = (input: number): string => {
	let dirty: boolean = false;
	let result: string = "";
	
	if (input % 2 === 0) {
		dirty = true;
		result += 'candy';
	}
	if (input % 11 === 0) {
		dirty = true;
		result += 'bar';
	}
	if (dirty === false) {
		result += input;
	}
	console.log(result);
    return result;
}
